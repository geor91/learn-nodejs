var phrases;

exports.connect = function () {
    phrases = require('./lang.json');
};

exports.getPhrase = function (phrase) {
    if (!phrases[phrase])
        throw new Error('Error. Not existing phrase name - ' + phrase);

    return phrases[phrase];
};